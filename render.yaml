services:
  - type: web
    name: moviellm-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false
    healthCheckPath: /
    autoDeploy: true 